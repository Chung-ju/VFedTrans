model_params:
  name: 'BetaVAE'
  # in_dim: 2500
  # latent_dim: 4900
  loss_type: 'B'
  gamma: 10.0
  max_capacity: 25
  Capacity_max_iter: 10000
  kld_weight: 0.00025
  
exp_params:
  LR: 0.0005
  weight_decay: 0.0
  scheduler_gamma: 0.95
  num_epochs: 50
  batch_size: 200
  enable_distill_penalty: True